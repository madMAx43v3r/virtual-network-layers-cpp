package vnl;

enum op_code_t {
	NOOP, MOV, CMP, ADD, SUB, MUL, DIV, MOD,
	PUSH, POP, LOAD, STORE, SHIFT_L, SHIFT_R,
	JMP, CALL, RET, READ, WRITE
}

struct code_t {
	op_code_t op;
	Var arg[2];
}

class Function {
	
	String name;
	List<String> params;
	List<Var> defaults;
	List<code_t> code;
	
}
