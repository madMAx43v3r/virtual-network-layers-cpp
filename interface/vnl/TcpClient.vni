package vnl;

object TcpClient extends TcpUplink {
	
	String endpoint;
	int port = 8916;
	
	bool autoclose = false;					// if client should exit on close
	bool tcp_nodelay = true;
	int send_buffer_size = 1048576;			// in bytes
	int receive_buffer_size = 1048576;		// in bytes
	
	void reconnect();						// force a new connection
	
}
